interface:
  display_name: "Prepare production readiness"
  short_description: "Bind release and operations evidence"
  default_prompt: "Use $prepare-production-readiness to build and verify the exact release and operating dossier."
policy:
  allow_implicit_invocation: true
