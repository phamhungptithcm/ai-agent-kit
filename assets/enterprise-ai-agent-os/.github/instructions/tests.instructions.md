---
applyTo: "test/**,tests/**,spec/**,**/*.test.*,**/*.spec.*"
---

Tests must prove the requested behavior and meaningful failure paths. Keep fixtures deterministic, isolate external systems, state platform-specific limitations, and never weaken an assertion only to make a gate pass. Link test evidence to the current repository state.
