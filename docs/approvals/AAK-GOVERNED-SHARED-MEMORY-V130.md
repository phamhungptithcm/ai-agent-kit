# Governed Shared Memory v1.3.0 Release Approval

- Plan: GitHub milestone #9 and issues #54 through #60
- Version: `1.3.0`
- Approver: `phamhungptithcm`
- Approval date: 2026-08-15

## Approved scope

The approved release includes the canonical `memory-entry-v3` contract,
transactional SQLite memory storage, evidence-bound subagent candidates,
independent promotion, scoped retrieval receipts, signed memory packs, the
remote adapter trust boundary, v2 migration, evaluations, and documentation.

## Publication authorization

The approver authorized creating a pull request, merging after required CI,
creating the annotated `v1.3.0` tag and GitHub Release, and allowing the release
workflow to publish the npm package with provenance.

## Constraints

- Preserve unrelated Product Genesis, Architecture Pulse, and team control
  plane work.
- Include no unrelated code in the release branch.
- Publish only after required CI passes and the merge, tag, GitHub Release, and
  npm package can be bound to the same source revision.

## Evidence boundary

This document records the user's explicit release authorization in the task.
It is not proof that the release occurred; live GitHub and npm records provide
publication evidence.
